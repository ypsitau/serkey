[package]
name = "serkey"
version = "0.1.0"
edition = "2021"

[features]
default = []
defmt = ["dep:defmt"]

[dependencies]
defmt = { version = "1.1.1", optional = true }
heapless = "0.9.3"
embedded-keycode = { git = "https://github.com/ypsitau/embedded-keycode" }
