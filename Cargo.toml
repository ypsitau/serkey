[package]
name = "serkey"
version = "0.1.0"
edition = "2021"

[features]
default = []
defmt = ["dep:defmt"]

[dependencies]
defmt = { version = "1.1.1", optional = true }
heapless = "0.9.3"

[target.'cfg(target_os = "linux")'.dependencies]
libc = "0.2"
